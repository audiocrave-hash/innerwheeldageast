name: Fetch Facebook Posts

on:
  schedule:
    # Daily at 06:00 UTC
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check token secret
        env:
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
        run: |
          if [ -z "$FB_PAGE_ACCESS_TOKEN" ]; then
            echo "::error::Secret FB_PAGE_ACCESS_TOKEN is not set."
            echo "Add it under Settings → Secrets and variables → Actions, then re-run."
            exit 1
          fi
          echo "Token secret is present."

      - name: Fetch posts from Graph API
        env:
          # Optional override; defaults to club page 61591944000616 in the script
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
          FB_POSTS_LIMIT: "12"
          FB_API_VERSION: "v21.0"
        run: python scripts/fetch_fb_posts.py

      - name: Commit updated posts.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/posts.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update Facebook posts from Graph API"
            git push
          fi
